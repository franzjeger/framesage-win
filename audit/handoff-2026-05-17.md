# Session handoff — 2026-05-17

**Purpose:** self-contained context for the next Claude session taking over the FrameSage v0.7 engagement. Everything load-bearing is captured here or in committed docs; the previous session's chat context is gone.

**Standing instructions remain in force.** See the established norms throughout `audit/` and `spike/` — particularly the ground rules around buddy review, no-unilateral-resolution of disagreements, and verification-commands-with-literal-output in spike-style reports.

---

## 0. Quick-start — read these in order

1. **This document** — picks the work back up at the current state.
2. **`audit/buddy-disagreements.md`** — the buddy-system audit trail (Entries 1, 2, 3). Documents the rhythm, the watch-signal evolution, and the user's process decisions.
3. **`spike/group-a-week-2-plan.md`** — APPROVED plan for Group A week 2. Day-by-day deliverables, stop gates, test inventory, code-level type specifications.
4. **`audit/buddy-format-implementation-phase.md`** — DRAFT format for code-review-phase buddy reviews. PR #75 is open; buddy raised two small (d) findings (see §3 below); the next session reviews the document directly before deciding whether to apply fixes or accept as-is.
5. **`audit/v0.7-architecture.md`** — Phase 2 architecture. Particularly §2.1 (degradation modes + build gate + EDR-interaction), §2.4 (Session History UI + honesty contract), and "Phase 3 acceptance criteria → Group A".
6. **`spike/etw-edr-report.md`** — Option-D resolution. §6.1 has the v0.7.1 default-on flip criteria (non-negotiable).
7. **`spike/etw-schemas.md`** — ETW kernel-event schema research (Group A week 1 deliverable).

---

## 1. State of play

### Plan state

- **Group A week 2 plan: APPROVED** (commit `5ca012d` on `plan/group-a-week-2`, PR #73 open). Revision history v1 → v2 → v3 → v4 → v4.1 → APPROVED preserved in §11. Buddy ran the four-question format on v3 (caught 4 substantive (d) findings) and v4 (caught 2 micro-(d) findings); user resolved each round explicitly and v4.1 applied the final two fixes plus four self-(d)-caught issues during application. Day 1 starts on PR #73 merge.

- **Implementation-phase buddy format: DRAFT** (commit `a0ab50f` on `plan/implementation-phase-buddy-format`, PR #75 open). Buddy ran the planning-phase 4Q on this DRAFT and verdict was **STOP-ON-(d)** with two small bibliographic findings (see §3). NOT FLIPPED to APPROVED per user instruction — next session reviews the document before sign-off.

### Code state

- `main` HEAD: `6fdc386` — PR #74 (02-sysinternals tightenings) merged. CI fully green; all rustfmt + clippy issues resolved via PRs #69 + #71.
- No production code shipped this session — all v0.7 Group A code is deferred to Day 1+ execution on `feat/group-a-week-2` (branch does not yet exist).

### v0.6 baseline state (status check 1, this session)

Audit confirmed v0.7 planning rests on a solid v0.6 baseline. All 9 items in `audit/PHASE2-PLAN.md` Group 1 shipped before any v0.7 work began (PR #65 was the first v0.7 spike, lands after PR #24 — the last Group 1 PR).

| Item | Plan ref | Shipped | Evidence (code-verified unless noted) |
|---|---|---|---|
| 1.1 Safe-list enforcement | §1.1 | YES | PR #16, `crates/gamemode/src/safe_lists/*.json` + `check_process_modifiable` at `engine/lib.rs:3451`, 8 unit tests |
| 1.2 ACL hardening | §1.2 | YES | PR #17 / `699a7f7`, `crates/service/src/acl.rs` |
| 1.3 SCM FailureActions + watchdog | §1.3 | YES | PR #18 / `b8ad963` |
| 1.4 Journal preservation | §1.4 | YES | PR #18 / `16b1c51`, `append_to_history` at `gamemode/journal.rs:284`, `sessions.jsonl` w/ `.1` rotation |
| 1.5 Uninstaller fix | §1.5 | YES + 2 follow-ups | PR #19 + PRs #63, #64 |
| 1.6 ProgramFiles install + migration | §1.6 | YES | PR #20 / `e0771a3` |
| 1.7 Consent gate | §1.7 + §4.1 | YES, with shape-divergence | PR #55. Plan called for `AppRule.game_mode = None` until opt-in; shipped routes to one of 3 pre-cloned profiles. User-facing contract identical. |
| 1.8 IPC line cap + Subscribe DoS | §1.8 | YES | PR #22 / `0196b3a`, `MAX_LINE_BYTES = 1MB`, `MAX_SUBSCRIBES = 32` per-PID |
| 1.9 AC detection + Safe Mode | §1.9 | YES | PR #23 / `0df8560`, `crates/sys/src/inner/ac_detect.rs`, `AntiCheatProfile` enum |

**User decision (this session):** kill switch is NOT a missing Group 1 item. The existing IPC surfaces `Request::GameModeOff` (Game-Mode-scoped panic / idempotent revert) and `Request::Pause`/`Resume` (engine-wide quiescence) cover the user's mental model. No v0.6.x patch needed. Possible v0.7+ polish: a single UI button that invokes both in sequence (deferred to future planning, not blocking Day 1).

---

## 2. PR #73 — Group A week 2 plan APPROVED

**Status:** Open, all commits pushed, ready to merge.

The next session's first call: review the actual APPROVED plan content (`spike/group-a-week-2-plan.md` on `plan/group-a-week-2` HEAD). If satisfied, merge PR #73 and Day 1 of Group A week 2 starts immediately on `feat/group-a-week-2` (branch to be created from `origin/main`).

**Day 1 deliverable (from plan §4 Day 1):** `crates/etw/` skeleton + `build_gate.rs` with `MIN_BUILD_FOR_CLOSED_LOOP = 26100` + `RtlGetVersion` wrapper + 3 unit tests. Stop gate if `RtlGetVersion` binding doesn't work — do NOT fall back to `GetVersionEx`.

**Important plan annotations to remember:**
- Plan §3.5 #5 surfaces a conflict with architecture §2.1 mode 5 (the consumer-panic disposition). Day 3 opens (NOT lands) a separate architecture-amendment PR. That amendment MUST land before Day 5's service-wiring code merges.
- Plan §3.6 specifies the `SupervisorLoop<F, S>` type that Day 3 scaffolds and Day 5 instantiates.
- Plan §12 specifies the EOD report template (`spike/group-a-week-2-report.md`). The asm-codegen-parity capture from Day 3 lands in §12.4 step 6.

---

## 3. PR #75 — implementation-phase buddy format DRAFT

**Status:** Open, NOT APPROVED per user instruction.

**Buddy ran planning-phase 4Q on the DRAFT (2026-05-17):**

| Question | Verdict | Notes |
|---|---|---|
| (a) Authority match | PASS | Five questions cover the user's four stated topics (FFI/concurrency/errors/tests); when-to-use gate sensibly scoped |
| (b) Scope correctness | PASS | Five is right size; (5) consistency is genuinely distinct from planning (d); process surface justified |
| (c) Realistic feasibility | PASS | Estimated 30-60 min per typical week-EOD PR; explicit enumeration mitigates hand-waving |
| (d) Internal consistency | **FAIL** | Two cross-reference findings (see below) |

**Buddy's two (d) findings:**

1. **Cross-branch dependency unresolved.** Line 4 of `audit/buddy-format-implementation-phase.md` cites Entries 2 & 3 of `audit/buddy-disagreements.md`, but those entries exist only on `plan/group-a-week-2` HEAD. On the format-doc's own branch (`plan/implementation-phase-buddy-format` @ `a0ab50f`), the file contains only Entry 1.

2. **§2.4 mis-attribution.** The self-pass at the bottom of the format doc claims `(§3.2, §3.4, §3.5, §3.6, §12, §6, §2.4) all point at sections that exist on plan/group-a-week-2 HEAD`. Five of six do; §2.4 does NOT. §2.4 is actually in `audit/v0.7-architecture.md`, not in `spike/group-a-week-2-plan.md`. The honesty-contract reference at line 72 is correctly to the architecture doc; only the self-pass bundles it wrong.

**Buddy's category-of-issue observation:**

> "Same category as Entries 2 and 3 — (d) cross-reference / internal-consistency findings... Count: 2 findings (down from Entry 2's 4 and Entry 3's 2). Severity: low — both are bibliographic, not load-bearing on the content. Trajectory: convergence (same category, reduced count, smaller scope) rather than growth. Per the revised watch-signal in the week-2 plan footer, this does NOT cross the threshold for structural intervention; recommend inline application by the next session rather than treating as something heavier."

Buddy's suggested follow-up note for future self-passes: "the self-pass should explicitly verify branch state, not just section existence."

**Disposition for next session:**

Per user instruction this session — DO NOT flip to APPROVED on buddy PROCEED alone. Next session reviews the document directly. Options when reviewed:
- α (matches buddy recommendation): apply the two fixes as small patches on the format-doc branch; flip APPROVED; merge.
- β: accept as-is (the findings are bibliographic; the format itself is sound).
- γ: amend the format substantively if the next session sees something buddy missed.

---

## 4. Outreach state (clocks running)

| # | Channel | Issue / link | Sent (UTC) | Day-5 cutoff (UTC) | Status |
|---|---|---|---|---|---|
| 01 | microsoft/perfview | [#2422](https://github.com/microsoft/perfview/issues/2422) | 2026-05-17 09:18:50 | **2026-05-22 09:18:50** | awaiting response |
| 03 | winsiderss/systeminformer | [#2916](https://github.com/winsiderss/systeminformer/issues/2916) | 2026-05-17 09:19:13 | **2026-05-22 09:19:13** | awaiting response |
| 02 | Sysinternals — body APPROVED | (not yet sent) | — | — | **HOLD: 48h waiting on PerfView contact route. Fallback: Tuesday 2026-05-19 EOD → techcommunity forum.** Verify URL before posting; Microsoft has been migrating from learn.microsoft.com/answers to techcommunity. |
| 04 | r/sysadmin | (user posts manually) | — | — | User's own Reddit account, not a primary-agent action. |

Log all send timestamps in `spike/etw-edr-report.md` §10.1. Log evidence-level responses in §10.2 as they arrive. Anecdotal "seemed fine" responses are NOT logged per §7's evidence-level requirement.

**Day-5 escalation rule (per `spike/etw-edr-report.md` §7):** if no evidence-level response by the listed cutoff, escalate that product to Option B (paid in-house validation) without further waiting.

**Tuesday 2026-05-19 EOD action item for next session:** check PerfView issue #2422 for any responder offering a personal contact route into Sysinternals. If found, use that channel for 02. If not, post 02 via Microsoft techcommunity forum and log §10.1.

---

## 5. Day 1 pre-conditions checklist

The next session can start Day 1 of Group A week 2 once all of these clear:

- [ ] PR #73 (plan APPROVED) merged into main
- [ ] PR #75 (implementation-phase format) reviewed by next session — applied/accepted/amended per next-session decision; merged or in flight
- [ ] Confirm CI on main is fully green at the latest commit (was green at session end at commit `6fdc386`)
- [ ] Create `feat/group-a-week-2` branch off latest `origin/main`
- [ ] Execute Day 1 per plan §4 (build gate + crate skeleton + 3 unit tests)

**Items that do NOT block Day 1 starting** (but become blockers later in the week):
- Architecture-amendment PR for §2.1 mode 5 (per plan §3.5 #5) — Day 3 deliverable, not Day 1
- 02 outreach send — orthogonal to code execution
- 04 r/sysadmin post — orthogonal to code execution

---

## 6. Decisions captured this session

For the audit trail:

1. **v0.6 kill switch** — closed. No v0.6.x patch. `GameModeOff` + `Pause` IPC surfaces sufficient. v0.7+ polish: single UI button invoking both in sequence (deferred, not tracked as a planning item yet).

2. **DRAFT v3 buddy findings (4 issues)** — user resolutions captured in `audit/buddy-disagreements.md` Entry 1 Resolution section + Entry 2:
   - d.1 generic shape → Option A (public generic-with-default), plus mandatory asm-codegen-parity capture in Day 3
   - d.2 stop-gate index → collapse §6 into one-line-per-day pointers; explicit drift-prevention rule
   - d.3 supervisor split → Day 3 scaffolds `SupervisorLoop<F, S>` type; Day 5 instantiates inside service crate
   - secondary channel question → Option C (defer channel wiring to Group C; week 2 emits via `tracing::error!`; Mode 5 test uses `tracing-test` capture)

3. **DRAFT v4 buddy findings (2 micro)** — user chose option α (apply as v4.1 patch + flip APPROVED). v4.1 also caught 4 additional issues during application (self-(d) pass).

4. **Watch-signal revision** — same-category STOP trigger now fires when (a) count similar/growing across drafts, OR (b) severity similar/growing, OR (c) self-pass empty while buddy still finds issues. v3 → v4 → v4.1 trajectory was convergence, not stagnation. Documented in `spike/group-a-week-2-plan.md` footer.

5. **Implementation-phase buddy format** — drafted as 5Q (FFI / concurrency / errors / tests / consistency). PR #75 open, NOT YET APPROVED.

6. **02 Sysinternals body** — APPROVED with three tightenings ("currently" replaces "2025-2026"; link target to §§1-3 not §6.1; generic salutation kept). Send schedule: hold 48h for PerfView contact route; Tuesday 2026-05-19 EOD fallback to techcommunity.

---

## 7. Standing instructions (carried forward)

- Spike-style reports and machine-state claims include verification commands + literal output, not just conclusions (per `spike/etw-edr-report.md` §8 + `audit/v0.7-architecture.md` Phase 3 ground rules).
- No "accept known risk and ship" decisions without the user.
- No ground-rule waivers.
- No scope expansion past what's in the architecture or the active plan.
- If a decision point comes up that wasn't anticipated, STOP and surface — don't guess.
- Buddy review on every substantive deliverable. Use planning-phase 4Q for documents; implementation-phase 5Q for code (once PR #75 is APPROVED).
- "Do not resolve disagreements unilaterally — that's what the user is for." If buddy raises concerns: STOP the affected workstream and write up in `audit/buddy-disagreements.md` with both perspectives.
- The four-question planning-phase format: (a) authority / (b) scope / (c) feasibility / (d) internal consistency.
- The five-question implementation-phase format (PENDING APPROVAL): (1) FFI safety / (2) concurrency / (3) error handling / (4) test-asserts-what-it-says / (5) internal consistency.
- Honesty contract for the closed-loop attribution UI (architecture §2.4) is non-negotiable.
- v0.7 supports Windows 11 24H2 (build 26100) and later only. Lower builds run v0.6 static-rule engine.
- EDR matrix validation is a v0.7.1 default-on-flip gate, NOT a v0.7 ship gate. v0.7 ships closed-loop default-off.
- v0.7.1 default-on flip requires ALL of `spike/etw-edr-report.md` §6.1 criteria: clean run on all 3 EDR products + gaming-load co-test + signed binary + vendor-allow-list path acceptable.

---

## 8. Local branches state (informational)

At session end, local repo has:
- Active work: `docs/handoff-2026-05-17` (this commit), `plan/group-a-week-2` (PR #73), `plan/implementation-phase-buddy-format` (PR #75) — all pushed to origin.
- Stale (safe to delete, not blocking): `chore/buddy-followups` (`d13406a` — local-only, superseded by merged PR #70), `research/v0.7-build-gate-and-edr-gap` (`f55ac7a` — local-only, superseded by merged PR #68).
- Older `feat/item-*` branches from v0.6 era still have origin tracking; all merged into main via their respective PRs. Cleanup optional.

Working tree clean except `.claude/` (local scripts, intentionally not committed).

---

## 9. What the next session should NOT do without explicit user direction

- Do NOT flip PR #75 to APPROVED on buddy PROCEED alone (this session's user instruction).
- Do NOT apply the two PR #75 (d) fixes unilaterally — review the document first, then choose α/β/γ per §3 above.
- Do NOT auto-merge PR #73 without reviewing the APPROVED plan content first.
- Do NOT iterate on the plan past v4.1 without user direction; convergence has been established.
- Do NOT post outreach 04 to r/sysadmin — that's the user's manual action.
- Do NOT skip buddy review on substantive deliverables, even small ones; the asymmetry between cheap-review and expensive-recovery is what makes the rhythm valuable.

---

**End of handoff.** Next session begins by reading this document and the docs listed in §0.
